// Mengekspor fungsi default dari modul ini
export default function (name) {
   // Fungsi ini menerima satu parameter bernama 'name'
   console.log(`Hello ${name}, from default function`); // Menampilkan pesan ke konsol dengan nama yang diberikan
}
